#!/usr/bin/env bash

if [ -z $QSMain ]; then
    echo "set QSMain=<reponame>"
    exit 0
fi

mv ~/.config/quickshell ~/.config/quickshell.bak/

pushd $HOME/.config

echo "Moving"
cp -r $QSMain/src ~/.config
mv ~/.config/src ~/.config/quickshell
echo "Moved"

popd
